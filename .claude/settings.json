{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const p=process.env.CLAUDE_TOOL_INPUT_FILE_PATH||''; const a=process.env.CLAUDE_AGENT_NAME||''; if(p.includes('/aegis/') && !a.startsWith('qa-') && a!=='') { console.error('TERRITORY VIOLATION: ' + a + ' attempted write to ' + p); process.exit(1); }\""
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [],
    "deny": []
  }
}
