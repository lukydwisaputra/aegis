# aegis/test-data/credentials/.gitignore
# Real credential files are local-only; only .example templates are committed.

*.env.local
*.env.local.*
*.local.*

# But keep:
!*.example
!*.example.*
!.gitignore
!README.md
