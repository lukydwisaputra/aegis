export default function AgentsPage() {
  return (
    <div className="space-y-4" data-testid="agents-page-container">
      <h1 className="text-xl font-semibold">Agents</h1>
      <p className="text-sm text-muted-foreground">Agent reliability scores and per-agent lessons will appear here after cycles run.</p>
    </div>
  );
}
