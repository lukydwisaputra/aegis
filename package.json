{
  "name": "aegis",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "qa-build-toc": "tsx scripts/build-toc.ts",
    "qa-check-onboarding-sync": "tsx scripts/check-onboarding-sync.ts",
    "diagram": "mmdc -i docs/D03-agent-workflow-diagram.md -o docs/D03-agent-workflow-diagram.svg"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.0.0",
    "@types/proper-lockfile": "^4.1.4",
    "eslint": "^9.0.0",
    "jest": "^29.7.0",
    "prettier": "^3.3.0",
    "ts-jest": "^29.1.2",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
